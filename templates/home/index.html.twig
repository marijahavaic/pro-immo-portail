{% extends 'base.html.twig' %}

{% block body %}
	<div class="hero">
		<img class="w-75 h-100 d-flex m-auto" src={{asset("images/hero.jpg")}} alt="">
	</div>

	<h1 class="text-center">Les biens disponibles à la vente et à la location:</h1>
	<div class="container row m-auto">
		{% for property in properties %}
			<div class="col-md-6 pt-3">
				<div class="card mb-3">
					<a href={{ path('app_property_show', {'id': property.id} ) }} class="text-decoration-none text-primary-emphasis">
						<div class="row g-0">
							<div class="col-md-4">
								<img class="img-fluid rounded-start" src={{asset(property.photo)}} alt='{{property.title}} image'>
							</div>
							<div class="col-md-8">
								<div class="card-body">
									<strong class="d-inline-block mb-0 text-success-emphasis">Louer</strong>
									<h3 class="mb-0">{{property.price}}€</h3>

									<div class="mb-1 text-body-secondary">{{property.createdAt|date('d/m/Y')}}</div>
									<p class="mb-auto">{{property.description|slice(0,45)}}...</p>
									<div>
										<strong>{{property.surface}}m²</strong>
										<p>{{property.town}},
											{{property.country}}</p>
									</div>
								</div>
							</div>
						</div>
					</a>

				</div>
			</div>

		{% endfor %}
	</div>
	<div class="d-flex justify-content-center mt-3">
		{{ knp_pagination_render(properties) }}
	</div>

	<div class="container row m-auto">
		<div class=" col-md-6">
			<div class="h-100 p-5 text-bg-dark rounded-3 map"></div>
		</div>
		<div class="col-md-6">
			<div class="h-100 p-5 bg-body-tertiary border rounded-3">
				<h2 class="text-center pt-5">A propos de nous :</h2>
				<p class="m-auto pt-2">Bienvenue chez Pro Immo Portail ! Votre passerelle vers le marché immobilier moderne. Nous sommes une agence
					<strong>innovante</strong>
					dédiée à simplifier la recherche de biens immobiliers, que ce soit pour la location ou l'achat. Notre plateforme en ligne offre une expérience sans pareille en mettant en relation des particuliers et des professionnels à travers des annonces de qualité. Notre mission est de rendre le processus de recherche et de transaction immobilière aussi transparent et efficace que possible.</p>

				<p class="pt-3">Ici vous trouverez des
					<strong>annonces de qualité</strong>
					grâce à notre séléction rigoureuse de professionnels de l'immobilier.</p>

				<p>
					<strong>Découvrez aujourd'hui la nouvelle façon de trouver votre chez-vous avec Pro Immo Portail !</strong>
				</p>
			</div>
		</div>
	</div>


<div>
<h4 class="text-center pt-4 pb-2">Contactez-nous :</h4>
	<div class="d-flex justify-content-center m-auto pb-5">
		{{ form_start(form) }}

		<div class="mb-3">
			<label for="contact_Nom">{{ form_label(form.Nom) }}</label>
			{{ form_widget(form.Nom, {'attr': {'class': 'form-control'}}) }}
			{{ form_errors(form.Nom) }}
		</div>
		<div class="mb-3">
			<label for="contact_Penom">{{ form_label(form.Prenom) }}</label>
			{{ form_widget(form.Prenom, {'attr': {'class': 'form-control'}}) }}
			{{ form_errors(form.Prenom) }}
		</div>
		<div class="mb-3">
			<label for="contact_Email">{{ form_label(form.Email) }}</label>
			{{ form_widget(form.Email, {'attr': {'class': 'form-control'}}) }}
			{{ form_errors(form.Email) }}
		</div>
		<div class="mb-3">
			<label for="contact_Telephone">{{ form_label(form.Telephone) }}</label>
			{{ form_widget(form.Telephone, {'attr': {'class': 'form-control'}}) }}
			{{ form_errors(form.Telephone) }}
		</div>
		<div class="mb-3">
			<label for="contact_Message">{{ form_label(form.Message) }}</label>
			{{ form_widget(form.Message, {'attr': {'class': 'form-control'}}) }}
			{{ form_errors(form.Message) }}
		</div>
		{{ form_end(form) }}
	</div>
</div>
{% endblock %}
